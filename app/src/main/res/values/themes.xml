<resources>
    <!-- Base application theme. -->
    <style name="Theme.FileManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:windowBackground">@color/gray_light</item>
    </style>

    <style name="myCV" parent="Widget.Material3.CardView.Filled">
        <item name="android:layout_marginStart">30dp</item>
        <item name="android:layout_marginEnd">30dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="cardCornerRadius">10dp</item>
        <item name="cardBackgroundColor">@color/white</item>
    </style>


    <style name="myInput" parent="ThemeOverlay.Material3.TextInputEditText">
        <item name="drawableTint">@color/gray_dark</item>
    </style>

    <style name="myIcon" >
        <item name="android:tint">@color/gray_dark</item>
    </style>


</resources>